# R1 screenshot reference

The immutable R1 review screenshots are in
[`../../ux-r1/new/`](../../ux-r1/new/). The images are referenced rather than
copied because the R2 comparison uses the same twelve captures.
